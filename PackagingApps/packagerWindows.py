#This will be run on a windows machine with github actions
#It will build the app for windows
#It will then package the app for windows as a single .exe that runs in the system terminal
#it will also create a folder with the jumperless python files, a simple launcher script (that checks for dependencies and installs them if needed), and a readme